import { ApplicationBootstrap } from './application-bootstrap';

describe('ApplicationBootstrap', () => {
  it('should create an instance', () => {
    expect(new ApplicationBootstrap()).toBeTruthy();
  });
});
